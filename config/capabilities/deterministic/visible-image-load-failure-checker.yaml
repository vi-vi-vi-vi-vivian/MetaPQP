id: visible-image-load-failure-checker
version: 1.0.0
kind: deterministic
supported_scopes: [page]
required_evidence: [evidence_elements, network]
implementation:
  entrypoint: "portal_audit.capabilities.checkers.page:VisibleImageLoadFailureChecker"
